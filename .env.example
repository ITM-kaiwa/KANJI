# Copy this file to .env.local for local development,
# and add the same two values in Vercel > Project Settings > Environment Variables.

# Supabase project URL (Project Settings > API > Project URL)
NEXT_PUBLIC_SUPABASE_URL=https://ltxqagtohxzjfrztbbdq.supabase.co

# Supabase "publishable" / anon key (safe to expose in the browser --
# read access is controlled by the Row Level Security policy in
# supabase/schema.sql, NOT by keeping this key secret).
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_1pitSKP5GbzLtSWDyAZ89g_aPog1IV8
